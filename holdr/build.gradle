apply plugin: 'com.android.library'

android {
    compileSdkVersion 25
    buildToolsVersion "25.0.2"

    defaultConfig {
        minSdkVersion 9

    }
}

dependencies {
    provided project(':holdr-stub')
    compile 'com.android.support:support-annotations:25.1.1'
}
